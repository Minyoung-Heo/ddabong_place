<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ezen.dda.store.StoreService">

<insert id="storeinput">
	insert into registration (store_id, storename, tel, address, lineintro, intro, image, main_menu, main_image, region_name, feature, dessert)
    values (#{store_id}, #{storename}, #{tel}, #{address}, #{lineintro}, #{intro}, #{image}, #{main_menu}, #{main_image}, #{region_name}, #{feature}, #{dessert})
</insert>

<select id="storeoutput" resultType="com.ezen.dda.store.StoreDTO">
	select * from registration
</select>

<select id="storemodifyview" resultType="com.ezen.dda.store.StoreDTO">
	select * from registration
</select>

<update id="storemodifyfinal">
	update registration set storename=#{storename}, tel=#{tel}, address=#{address}, lineintro=#{lineintro}, intro=#{intro}, image=#{image}, main_menu=#{main_menu}, main_image=#{main_image}, region_name=#{region_name}, feature=#{feature}, dessert=#{dessert} where store_id=#{store_id}
</update>


</mapper>